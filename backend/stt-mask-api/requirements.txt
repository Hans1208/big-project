# stt-mask-api (대면 상담 STT + 개인정보 마스킹) 로컬 실행용.
#
# 운영은 Modal(modal/backend.py)에서 GPU로 돌지만, 그쪽은 이미지 정의에 의존성이
# 인라인으로 박혀 있어 로컬에서 app/main.py를 띄울 근거가 없었다. 이 파일이 그 역할.
# 버전은 modal/backend.py의 uv_pip_install 목록과 같은 패키지를 쓰되, 로컬은 GPU가
# 없는 경우가 많아 CPU 휠로 충분하다.
#
# ffmpeg는 pip로 안 들어온다 — pydub이 webm/opus를 디코딩할 때 외부 실행파일로 부른다.
# 없으면 매 조각이 CouldntDecodeError로 실패한다. Windows는 `winget install Gyan.FFmpeg`.
fastapi
uvicorn[standard]
python-multipart
numpy
faster-whisper
pydub
transformers
torch
